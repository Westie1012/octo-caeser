function encrypt(){	//grabbing the variables from html	var value = parseInt(document.getElementById('value').value);	var start = document.getElementById('start').value;		var end = ""	for (var i =0; i<=start.length;i++) {		var letter = start.charAt(i);		end = end + String.fromCharCode(String.charCodeAt(letter)+value);	}	document.getElementById('test').innerHTML=end;}function decrypt(){	//grabbing the variables from html	var value = parseInt(document.getElementById('value').value)*-1;	var start = document.getElementById('start').value;		var end = ""	for (var i =0; i<=start.length;i++) {		var letter = start.charAt(i);		end = end + Sstring.fromCharCode(String.charCodeAt(letter)+value);	}	document.getElementById('test').innerHTML=end;}